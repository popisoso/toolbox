/**
 * App identity. PLACEHOLDER — the real name/icon are the owner's call.
 * Changing the name here + in public/manifest.webmanifest + index.html
 * <title> is the complete rename; icons regenerate via `npm run icons`.
 */
export const APP_NAME = 'Toolbox';
